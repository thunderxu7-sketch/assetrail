# Use at least 32 random bytes in production.
TRANSFER_COOKIE_SECRET=replace-with-a-long-random-secret

# Protects the allowlisted cache revalidation route.
REVALIDATION_TOKEN=replace-with-an-independent-random-token

# Canonical production URL, without a trailing slash.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
